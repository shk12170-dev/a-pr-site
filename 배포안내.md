# 배포 안내 (GitHub → Vercel 2개)

사이트가 두 개라 **Vercel 프로젝트도 두 개**를 만듭니다. 저장소는 하나만 있으면 됩니다.

## 1. GitHub에 올리기

```bash
cd "과제 A 나를 소개하는 사이트"
git init
git add .
git commit -m "과제 A: 자기소개 사이트 2개와 갱신 장치"
git branch -M main
git remote add origin https://github.com/shk12170-dev/<저장소이름>.git
git push -u origin main
```

`참고자료/`(다른 과제 저장소를 복제해 둔 폴더)와 `점검어.txt`는 `.gitignore`에 들어 있어 올라가지 않습니다.
`git status`로 `참고자료/`가 목록에 없는지 한 번 확인하고 푸시하세요.

## 2. Vercel 프로젝트 2개 만들기

두 프로젝트 모두 같은 저장소를 고르고, **Root Directory만 다르게** 설정합니다.

| 프로젝트 | Root Directory | Framework Preset |
|---|---|---|
| 백엔드 버전 (제출용) | `site-backend` | Other |
| 클라우드 버전 | `site-cloud` | Other |

빌드 명령과 설치 명령은 비워 둡니다. 정적 HTML이라 빌드가 필요 없습니다.

## 3. 주소를 적고 장치를 한 번 돌리기

배포가 끝나면 `본문/배포주소.json`을 채웁니다.

```json
{
  "backend": "https://<백엔드주소>.vercel.app",
  "cloud": "https://<클라우드주소>.vercel.app",
  "제출할_트랙": "backend",
  "소스저장소": "https://github.com/shk12170-dev/<저장소이름>"
}
```

그다음 아래를 순서대로 실행하면 사이트의 상호 링크, 짧은 확인 방법, 제출문의 주소가 한 번에 채워집니다.

```bash
python 장치/build.py
python 장치/묶기.py
python 장치/점검.py
```

`점검.py`가 `통과 15 · 대기 0 · 실패 0`이 되면 제출 준비가 끝난 것입니다.
(주소를 채운 뒤에도 BRA-C13은 "직접 확인"으로 남습니다. 새 시크릿 창에서 두 주소를 직접 열어보세요.)

## 4. 마지막에 다시 푸시

`build.py`가 사이트 HTML을 다시 만들기 때문에, 주소를 채운 뒤 한 번 더 커밋해야 상호 링크가 실제 사이트에 반영됩니다.

```bash
git add .
git commit -m "배포 주소 반영"
git push
```

## 제출할 것

| 항목 | 파일·주소 |
|---|---|
| 결과물 URL (한 개) | 백엔드 사이트 주소 |
| 문서·장치 | `제출물/제출_문서와장치.zip` |
| 짧은 확인 방법 | `제출물/짧은_확인_방법.md` |
| 제출문 (AI와 나의 판단 3줄) | `제출물/제출문.md` |
