# 배포 안내 (GitHub → Vercel)

이 저장소(`a-pr-site`)는 **백엔드 개발자 버전 사이트 하나**만 담고 있습니다.
클라우드 엔지니어 버전은 바탕화면의 `과제 A 클라우드 엔지니어 버전` 폴더에 따로 있습니다.

## 1. Vercel 프로젝트 만들기

Vercel에서 `shk12170-dev/a-pr-site` 저장소를 가져오고 아래처럼 설정합니다.

| 항목 | 값 |
|---|---|
| Root Directory | `site-backend` |
| Framework Preset | Other |
| Build / Install Command | 비워 둠 (정적 HTML이라 빌드 불필요) |

**Root Directory를 꼭 `site-backend`로 지정하세요.** 지정하지 않으면 저장소 최상단이 열려 `장치/`, `문서/` 같은 폴더 목록이 그대로 보입니다.

## 2. 주소를 적고 장치를 한 번 돌리기

배포가 끝나면 `본문/사이트설정.json`의 `배포주소`를 채웁니다.

```json
{
  "트랙": "backend",
  "배포주소": "https://<배포주소>.vercel.app",
  "소스저장소": "https://github.com/shk12170-dev/a-pr-site"
}
```

그다음 아래를 순서대로 실행하면 짧은 확인 방법과 제출문의 주소가 채워집니다.

```bash
python 장치/build.py
python 장치/묶기.py
python 장치/점검.py
```

## 3. 다시 푸시

```bash
git add .
git commit -m "배포 주소 반영"
git push
```

## 4. 새 시크릿 창에서 확인 (BRA-C13)

배포 주소를 새 시크릿 창에서 열어 로그인 없이 첫 화면이 뜨는지, 대표작의 `논문 파일 내려받기`가 열리는지 확인합니다.

## 제출할 것

| 항목 | 파일·주소 |
|---|---|
| 결과물 URL (한 개) | 위에서 배포한 주소 |
| 문서·장치 | `제출물/제출_문서와장치.zip` (`python 장치/묶기.py`로 생성, 저장소에는 올리지 않음) |
| 짧은 확인 방법 | `제출물/짧은_확인_방법.md` |
| 제출문 (AI와 나의 판단 3줄) | `제출물/제출문.md` |
